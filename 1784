class Solution {
public:
    bool checkOnesSegment(string s) {
        int n = s.size();
        // int i = 1;
        // while(i<n)
        for(int i=1;i<n;i++){
            if(s[i]=='1' && s[i-1]=='0'){
                return false;
            }
        }
return true;

    }
};
